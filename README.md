# nvim-config

GAchuzia's nvim config

## Notes to Self

Symlinked %LOCALAPPDATA%\nvim to this repo so Neovim loads its config directly using the command below.

``` powershell 
New-Item -ItemType SymbolicLink -Path "$env:LOCALAPPDATA\nvim" -Target "C:\ChukwudubanyiGrantAchuzia\Code\nvim-config"
```
